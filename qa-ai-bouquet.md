# AI Bouquet QA notes

- 2026-08-21: The circular composition, staged placement classes, 5-second minimum generation duration and loading `aria-busy` state are verified by TypeScript and Vitest source contracts.
- 2026-08-21: The My Browser dev-session Builder page rendered the Visual Builder default panel, but AI tab clicks and keyboard activation only moved focus without changing the displayed panel. No fresh client-console error was available in the local dev logs. Continue visual layout checks in the managed preview instead of treating this browser-session behavior as a component regression.
- 2026-08-21: Managed preview screenshots at 375px and 1440px confirm the Builder page has no new horizontal overflow or layout regression from the AI composition CSS. The current session could not switch the live browser into the AI panel, so the circular arrangement itself remains covered by source-contract and build validation rather than a rendered screenshot in this session.
- 2026-08-21: ContactStrip map replacement keeps the 375px and 1440px layouts within their containers. The iframe deliberately uses lazy loading; in an initial full-page capture its map tiles can remain unloaded until the contact section enters the viewport, while the directions action remains immediately visible.
- 2026-08-21: Corrected reference QA confirms ContactStrip is restored to the compact dark/cream two-panel shape. The About contact card now reserves the former bouquet area for the map and removes the large lower map row. The first full-page capture showed the lazy iframe frame before its tiles; the About card map is consequently eager-loaded so the requested map surface is present without waiting for a scroll trigger.
- 2026-08-21: In the browser session, other About images appeared after the initial load settled. A scroll-to-end request did not move the viewport to the lower contact card, so direct visual map-tile inspection could not be completed there; the map relocation, dimensions and removal of the old full-width row remain covered by source contracts, TypeScript and build validation.
- 2026-08-21: The current OpenStreetMap embed URL opens and renders its marker/control frame, but its tile layer is blank in the browser environment. Do not treat that provider as visually sufficient for the replacement surface; verify an alternative embed before the next corrected checkpoint.
- 2026-08-21: The public Google Maps URL resolves to an embed-only destination when opened directly, which confirms it is designed for the iframe use in the About contact card. The centralized `brand.mapEmbedUrl` now uses this provider in place of the visibly blank OpenStreetMap tile layer.
- 2026-08-21: To avoid leaving the user with a blank provider frame in this environment, the About card now uses the managed static map visual extracted from the user's supplied reference, while retaining the external directions link for interactive navigation. The map visual is hosted in project static storage and does not depend on third-party tile loading.
- 2026-08-21: Final managed-preview checks at 1440px and 375px show the map visibly occupies the former About-card bouquet area, the former large lower map row is absent, and the pre-footer ContactStrip is once again the compact dark/cream card from the first reference. No horizontal overflow is visible at either checked width.
- 2026-08-21: The user confirmed that a static map visual is insufficient. The About card now returns to the centralized Google Maps iframe with eager loading and fullscreen capability, so the left panel supports native map pan and zoom; the directions link remains as a secondary navigation route.
